[package]
name = "tprobe"
version = "0.1.5"
edition = "2024"
description = "Tick-counting performance probe for short code-path measurement, vendored from iiac-perf."
license = "MIT OR Apache-2.0"

[dependencies]
core_affinity = "0.8"
hdrhistogram = "7"
minstant = "0.1"
